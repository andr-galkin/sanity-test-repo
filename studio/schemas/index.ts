import blockContent from './blockContent'
import category from './category'
import portfolio from './portfolio'
import author from './author'

export const schemaTypes = [portfolio, author, category, blockContent]
